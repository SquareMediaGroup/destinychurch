// @destiny/shared — types and framework-agnostic logic shared between the web
// app (`app/`, `lib/`), the mobile app (`mobile/`), and the app BFF
// (`app/api/app/*`). Keep this package free of framework-specific imports
// (no `next`, no React, no react-native) so every consumer can use it.

export * from "./churchsuite/events";
