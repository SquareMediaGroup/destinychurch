// whisper.h's own directory is added via HEADER_SEARCH_PATHS/unsafeFlags in
// Package.swift, since it lives in the gitignored Vendor/whisper.cpp build
// output rather than being copied into this package.
#include "whisper.h"
