# packages/shared

Placeholder for types and validation schemas shared between the web app
(`app/`, `lib/`) and the mobile app (`mobile/`) — e.g. sermon/podcast
episode types, ChurchSuite response shapes, zod schemas.

No code yet — see `docs/mobile-app-scope.md`.
